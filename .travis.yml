language: java

git:
  quiet: true

script:
  - echo "salam!!!"
#  - mvn clean

jdk:
  - openjdk8

before_install:
  - sudo chmod 777 .travis/before.sh

install: true

before_script:
  - .travis/before.sh
  - sleep 5

cache:
  directories:
    - $HOME/.m2
    - $HOME/Downloads

after_success:
  - bash <(curl -s https://codecov.io/bash)
